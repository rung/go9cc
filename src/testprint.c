#include<stdio.h>

int foo() { printf("foo output:\n"); };
int bar(int a, int b, int c) { printf("bar output: %d, %d, %d\n", a, b, c); };
int barr(int a, int b, int c, int d, int e, int f, int g, int h, int i) { printf("bar2 output: %d, %d, %d, %d, %d, %d, %d, %d, %d\n", a, b, c, d, e, f, g, h, i); };
